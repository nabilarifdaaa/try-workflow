states:
  registered:
      transition: "review_cv"
  review_cv:
      transitions:
        tes_teknis:
          condition: "status_cv == 'approved'"
        tolak:
          condition: "status_cv != 'approved'"
  tes_teknis:
      transitions:
        terima:
          condition: "statusTesTeknis == 'approved'"
        tolak:
          condition: "statusTesTeknis != 'approved'"
  terima:
    end: true
  tolak:
    end: true
    